# Cargo generated files (build)
debug/
target/

# Remove if compiling shipped binaries
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

#macOS artifacts
.DS_Store

# Metadata, debugging, dev work, tooling
.cert/
.config/
.tools/
